# ~/.tmuxinator/usnodes.yml


name: usnodes
root: ~/

windows:
  - 1to4:
      layout: tiled
      panes:
        - ssh austin
        - ssh philly
        - ssh baltimore
        - ssh chicago
        - ssh indianapolis
        - ssh houston
        - ssh sanantonio
